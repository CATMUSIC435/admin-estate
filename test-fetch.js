const query = 'query { locations { id name slug } properties { id name location transaction_type price } blogs { id title slug } projects { id name slug } }'; fetch('https://api.alizedanang.net/graphql', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({query}) }).then(r => r.json()).then(d => console.log(JSON.stringify(d).substring(0, 500))).catch(console.error);
